<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="app-background"></div>

  <% if user_signed_in? && (@streak_count.to_i > 0 || @max_streak_count.to_i > 0) %>
    <div class="streak-banner" role="status" aria-live="polite">
      <% if @streak_count.to_i > 0 %>
        <span class="chip chip-current">現在⚽️ <strong><%= @streak_count %></strong> 連ゴール🔥</span>
      <% end %>
      <% if @max_streak_count.to_i > 0 %>
        <span class="chip chip-max">最高記録🏆 <strong><%= @max_streak_count %></strong> 連ゴール！</span>
      <% end %>
    </div>
  <% end %>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
